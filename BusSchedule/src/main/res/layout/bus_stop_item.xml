<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?android:attr/selectableItemBackground"
    android:paddingHorizontal="@dimen/bus_stop_item_horizontal_padding"
    android:paddingVertical="@dimen/bus_stop_item_vertical_padding">

    <TextView
        android:id="@+id/stop_name_text_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="2"
        android:textSize="20sp"
        tools:text="Main Street" />

    <TextView
        android:id="@+id/arrival_time_text_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:gravity="end"
        android:textSize="20sp"
        android:textStyle="bold"
        tools:text="10:00 PM" />

</LinearLayout>
